<!DOCTYPE html>
html(lang="en")
  head
    meta(charset="utf-8")
    title Express Gallery
    link(rel = 'stylesheet', href = '/css/galleryStyles.css')
  body
    div(class = 'galleryPage')
      hr
      div(class = 'galleryPageHeader')
        div(class = 'galleryTitle')
          h1 _ARCHITEKT
        div(class = 'galleryWelcome')
          span Welcome to our online portfolio, we are a small group of passionate designers and architects, looking to change and create amazing digital images to inspire others to follow in our innovative steps.
      br
      br
      br

      div(class = 'galleryListing')
        ul
          each val, index in json
            if index === 0
              div(class = 'displayLeft')
                li(class = 'galleryFirstImageContainer')
                  img(src = val.url class = 'frontPagePicture')
                  br
                  form(action="/gallery/" + id + "?_method=delete", method = "post", id = "form")
                    button(type="submit" class = "deleteButton") Delete picture #{id}
                  hr
            else
              div(class = 'displayRight')
                li(class = 'galleryImageContainer')
                  img(src = val.url class = 'frontPagePicture')
                  div(class = 'pictureInfo')
                    h4 #{val.title}
                    p Uploaded by: #{val.author}
                    p #{val.description}
